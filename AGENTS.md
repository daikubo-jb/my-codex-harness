# Codexハーネス共通ルール

日本語で応答する。

## 言語方針

- ユーザーへの最終回答と確認事項は日本語で返す。
- `AGENTS.md`、Skill、Agent定義の説明は、ユーザーやリポジトリの文脈を正確に扱える言語を使う。既存の日本語指示を英語へ一括変換しない。
- Agent間で安定した構造化出力を使う場合、項目名・状態名・識別子は英語にしてよい。内容と根拠は日本語でもよい。
- 英語での内部推論やchain-of-thoughtの出力を要求しない。思考過程の代わりに、観測事実、根拠、判断、不確実性、検証結果を返す。

## 作業の所有

メインエージェントが依頼を end-to-end で所有する。リポジトリの証拠、既存の手順、テスト、最終差分を判断の根拠にし、探索 → 必要な計画 → 実装 → 検証 → 修正 → 完了確認まで進める。可逆的な局所判断は決めて進み、根拠が変わったときだけ再考する。

通常のコーディングを職種別エージェントへ分割しない。大量の読み取り、独立した検証、または別視点の確認がメインの文脈を保つ場合だけ委譲する。委譲先には目的、対象範囲、入力、期待する出力、待ち合わせ条件を明示する。複数エージェントが同じコードを並行して編集しない。

## 作業量とリスクに応じた手順

- 局所的・可逆で要件が明確な変更は、メインで実装し、関連する検証と差分確認を行う。spec、plan-reviewer、test-runner、記録を必須にしない。
- 複数箇所の整合が必要な変更は、必要なら短い計画を作り、実装、検証、完成物レビュー、修正後の再検証を行う。
- API互換性、DB移行、認証・認可、秘密情報、不可逆な変更は、影響範囲・互換性・移行またはrollback・検証方法を計画し、plan-reviewerと完成物Reviewerを必要な節目で使う。
- repoの証拠を調べても結論が分かれる、影響の大きい局所判断、または異なる実装案のtrade-offが重要な場合は`advisor`へsecond opinionを求める。Advisorは命令や承認の代わりにならない。
- システム境界、domain分割、長期migration、認証モデルの根本変更、またはrollbackコストが大きい判断は`senior-advisor`へ相談する。Senior Advisorは`gpt-6-astra`の`medium`を使うread-onlyの例外経路で、通常の実装や局所判断には使わない。
- ユーザーの意図や許容する成果物が未確定で、判断が成果物を変える場合はspecで確定してから実装する。

AdvisorやReviewerの助言はsecond opinionであり、ユーザーの承認やリポジトリの証拠を置き換えない。指摘を採用するかはメインが根拠とともに判断する。

## 検証と完了

検証コマンドは対象プロジェクトの`AGENTS.md`、`CLAUDE.md`、`.agents/skills/run-*`、既存設定、CIの順に探す。実行していないものを成功扱いしない。

検証結果は項目ごとにコマンド、対象、結果を示す。

- 成功: 実行して通過した。
- 失敗: 実際の出力と、今回の変更との関係を示す。不明なら不明とする。
- 未実行: 環境不足などの理由と再開条件を示す。
- 対象外: 今回不要な理由を示す。

完了前に、ユーザーの目的と受け入れ条件、関連テスト、必要なlint/typecheck/build、最終差分を確認する。rename・migration・API変更では古い参照や移行漏れを検索する。画面や動作の確認が必要なら、テスト成功だけで代替しない。必須検証が失敗・未実行なら、検証未完了として報告する。

テストを削除・緩和して失敗を隠さない。既存の不具合を依頼範囲へ無断で追加しない。不要なリファクタや一般知識を再教育するだけのSkillを追加しない。

## 状態と記録

会話だけを作業状態のsource of truthにしない。長時間・高リスク・ユーザーが明示した引き継ぎでは、doc-storageまたはプロジェクトの既存文書規約に、対象リポジトリとブランチ/コミット、目的、完了条件、決定事項と根拠、実施済み作業、検証結果、未解決事項、次の操作を記録する。小変更の記録は必須にしない。

再開時は記録を現在のブランチと差分に照合し、古い記録をそのまま信頼しない。記録できない場合は、その事実を報告する。

## ハーネスの自己改善

作業中にハーネスの指示、hook、rules、AgentやReviewerの運用について示唆が得られたら、worklogの末尾にある「ハーネスへの示唆」へ観測事実として記録する。該当がない場合も「なし」と記録し、欄自体は省略しない。

ハーネスの変更候補は、明示起動の`$harness-review`だけが提案する。作業中のセッションが自分でハーネスを変更してはならない。提案にはworklogのパスと日付、発生回数、影響、候補となる変更先を含め、ユーザーが採用を選んだものだけをメインエージェントが適用する。

繰り返しは昇格の候補条件であって自動適用条件ではない。機械的に防げる操作はrulesまたはhook、不変条件は検証スクリプト、手順の抜けはSkill、判断や意図は指示文へ寄せる。重大な事故は1件でも候補にできるが、摩擦と効果を比較して採否を決める。

## 参照

- 中規模以上の変更: `$feature`
- 未確定の大きな依頼: `$spec`
- 計画の批評: `$plan-review`
- 直近の記録: `$doc`
- 文書配置と再開記録: `$doc-storage`
- ハーネス改善候補の提案: `$harness-review`
- 完成物の読み取り専用確認: `agents/code-reviewer.toml`
- 難しい局所判断の相談: `agents/advisor.toml`
- 高影響なアーキテクチャ判断: `agents/senior-advisor.toml`
