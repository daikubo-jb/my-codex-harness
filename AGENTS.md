# 共通ルール

日本語で応答する。

## 実装と委譲

実装はメインエージェントが担当する。サブエージェントへの委譲は、出力が大量でメインの文脈を埋める場合、または別の視点で検証する価値がある場合に限る。

- テストの作成・実行 → `test-runner`
- 広範囲の調査 → 組み込みの `explorer` または読み取り専用のサブエージェント
- プランの批評 → `plan-reviewer`
- ドキュメントの記録 → `doc-writer`

数回のツール呼び出しで終わる作業は委譲しない。並列エージェントを使う場合は、独立した読み取り・検証作業に限定する。

## 作業の進め方

依頼された範囲を実装する。頼まれていないリファクタや抽象化は足さない。仕様の判断に迷ったら妥当な方を選んで進め、選択理由を報告する。成果物が変わる判断だけ質問する。

何を作るかが固まっていない大きめの依頼では、`$spec` を使って先に仕様を固める。差分を1文で説明できる依頼には使わない。

テストが落ちているなら、実際の出力とともに報告する。実行していない手順を実行したことにしない。

## 応答の書き方

結論から書く。最初の1文で「何が起きたか」「何が分かったか」に答える。根拠と詳細はその後に書く。単純な質問には単純に答える。

## ドキュメント

プロジェクトのドキュメントは、リポジトリ内で管理する慣習がない場合、`doc-storage` に集約する。配置ルートと規約は `$doc-storage` を読む。

## 検証

ビルド・テスト・lint のコマンドは、プロジェクトの `AGENTS.md`、`CLAUDE.md`、または `.agents/skills/run-*` にあるものを使う。見つからなければ、既存設定から実行可能な検証方法を調べ、推測で成功扱いにしない。
